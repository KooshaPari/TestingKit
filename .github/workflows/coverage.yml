# Generated coverage workflow - calls reusable phenotype workflow
permissions:
  contents: read
  pull-requests: read
name: Coverage

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  coverage:
    uses: KooshaPari/phenotype-infrakit/.github/workflows/coverage.yml@0000000000000000000000000000000000000000 # placeholder SHA; source 404s; replace when reachable
    secrets: inherit
